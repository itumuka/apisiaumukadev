services:
  laravel-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-siadev-be
    restart: unless-stopped
    ports:
      - "8001:8001"
    volumes:
      - .:/var/www/html
    environment:
      APP_ENV: production
      APP_DEBUG: "true"
      APP_KEY: "base64:gWPyx4GYcMtiiqFcvZkm5Vb/1uSDpmZmz//HcbW1K/A="
      APP_URL: "http://210.79.191.206:8001"
      SECOND_URL: "https://api-siadev.umuka.ac.id/api/"
      THIRD_URL: "https://api-siadev.umuka.ac.id/"
      SIEDOM_URL: "https://siedom.umuka.ac.id/"
    networks:
      - api-siadev-net

networks:
  api-siadev-net:
    driver: bridge